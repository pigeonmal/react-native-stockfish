///
/// HybridStockfishEngineSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `BestMoveResult` to properly resolve imports.
namespace margelo::nitro::stockfish { struct BestMoveResult; }
// Forward declaration of `SearchOptions` to properly resolve imports.
namespace margelo::nitro::stockfish { struct SearchOptions; }
// Forward declaration of `ListenerSubscription` to properly resolve imports.
namespace margelo::nitro::stockfish { struct ListenerSubscription; }
// Forward declaration of `AnalysisInfo` to properly resolve imports.
namespace margelo::nitro::stockfish { struct AnalysisInfo; }

#include <NitroModules/Promise.hpp>
#include <string>
#include <vector>
#include "BestMoveResult.hpp"
#include "SearchOptions.hpp"
#include <optional>
#include "ListenerSubscription.hpp"
#include "AnalysisInfo.hpp"
#include <functional>

namespace margelo::nitro::stockfish {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `StockfishEngine`
   * Inherit this class to create instances of `HybridStockfishEngineSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridStockfishEngine: public HybridStockfishEngineSpec {
   * public:
   *   HybridStockfishEngine(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridStockfishEngineSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridStockfishEngineSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridStockfishEngineSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<void>> setPosition(const std::string& fen, const std::vector<std::string>& moves) = 0;
      virtual std::shared_ptr<Promise<BestMoveResult>> startSearch(const std::optional<SearchOptions>& options) = 0;
      virtual void stop() = 0;
      virtual std::shared_ptr<Promise<void>> clearHash() = 0;
      virtual ListenerSubscription addOnAnalysisInfoListener(const std::function<void(const AnalysisInfo& /* info */)>& listener) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "StockfishEngine";
  };

} // namespace margelo::nitro::stockfish
