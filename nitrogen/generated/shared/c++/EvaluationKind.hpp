///
/// EvaluationKind.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::stockfish {

  /**
   * An enum which can be represented as a JavaScript union (EvaluationKind).
   */
  enum class EvaluationKind {
    CENTIPAWNS      SWIFT_NAME(centipawns) = 0,
    MATE      SWIFT_NAME(mate) = 1,
    TABLEBASE      SWIFT_NAME(tablebase) = 2,
  } CLOSED_ENUM;

} // namespace margelo::nitro::stockfish

namespace margelo::nitro {

  // C++ EvaluationKind <> JS EvaluationKind (union)
  template <>
  struct JSIConverter<margelo::nitro::stockfish::EvaluationKind> final {
    static inline margelo::nitro::stockfish::EvaluationKind fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("centipawns"): return margelo::nitro::stockfish::EvaluationKind::CENTIPAWNS;
        case hashString("mate"): return margelo::nitro::stockfish::EvaluationKind::MATE;
        case hashString("tablebase"): return margelo::nitro::stockfish::EvaluationKind::TABLEBASE;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum EvaluationKind - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::stockfish::EvaluationKind arg) {
      switch (arg) {
        case margelo::nitro::stockfish::EvaluationKind::CENTIPAWNS: return JSIConverter<std::string>::toJSI(runtime, "centipawns");
        case margelo::nitro::stockfish::EvaluationKind::MATE: return JSIConverter<std::string>::toJSI(runtime, "mate");
        case margelo::nitro::stockfish::EvaluationKind::TABLEBASE: return JSIConverter<std::string>::toJSI(runtime, "tablebase");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert EvaluationKind to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("centipawns"):
        case hashString("mate"):
        case hashString("tablebase"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
